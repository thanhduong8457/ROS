cmake_minimum_required(VERSION 3.14.4)
project(my_delta_robot)

if(NOT CMAKE_CXX_STANDARD)
  set(CMAKE_CXX_STANDARD 17)
endif()
if(CMAKE_COMPILER_IS_GNUCXX OR CMAKE_CXX_COMPILER_ID MATCHES "Clang")
  add_compile_options(-Wall -Wextra -Wpedantic)
endif()

find_package(ament_cmake REQUIRED)
find_package(rclcpp REQUIRED)
find_package(std_msgs REQUIRED)
find_package(sensor_msgs REQUIRED)
# find_package(message_generation REQUIRED)
# find_package(genmsg REQUIRED)
find_package(urdf REQUIRED)
# find_package(serial REQUIRED)

## Generate messages in the 'msg' folder
# add_message_files(FILES
#   angulo.msg
#   linear_speed_xyz.msg
#   posicionxyz.msg
#   PositionArm.msg
#   image_pos.msg
#   vmax_amax.msg
#   num_point.msg
# )

## Generate services in the 'srv' folder
# add_service_files(
#   FILES
#   Service1.srv
#   Service2.srv
# )

## Generate actions in the 'action' folder
# add_action_files(
#   FILES
#   Action1.action
#   Action2.action
# )

rosidl_generate_interfaces(${PROJECT_NAME}
    angulo.msg
    linear_speed_xyz.msg
    posicionxyz.msg
    PositionArm.msg
    image_pos.msg
    vmax_amax.msg
    num_point.msg
    DEPENDENCIES actionlib_msgs std_msgs geometry_msgs
)

## Generate added messages and services with any dependencies listed here
# generate_messages(
#  DEPENDENCIES
#  std_msgs
# )


## Specify additional locations of header files
## Your package locations should be listed before other locations
include_directories(include ${catkin_INCLUDE_DIRS} include/my_delta_robot)

add_executable(
    main_node 
    src/main_node.cpp 
    src/common.cpp 
    src/delta_robot.cpp)
target_link_libraries(main_node ${std_msgs_TARGETS})
# add_dependencies(main_node my_delta_robot_generate_messages_cpp ${catkin_EXPORTED_TARGETS})

add_executable(node_b src/node_b.cpp)
target_link_libraries(node_b ${std_msgs_TARGETS})
# add_dependencies(node_b my_delta_robot_generate_messages_cpp ${catkin_EXPORTED_TARGETS})

add_executable(node_a src/node_a.cpp)
target_link_libraries(node_a ${std_msgs_TARGETS})
# add_dependencies(node_a my_delta_robot_generate_messages_cpp ${catkin_EXPORTED_TARGETS})

add_executable(test_node_a src/test_node_a.cpp)
target_link_libraries(test_node_a ${std_msgs_TARGETS})
# add_dependencies(test_node_a my_delta_robot_generate_messages_cpp ${catkin_EXPORTED_TARGETS})

add_executable(serial_module src/serial_module.cpp)
target_link_libraries(serial_module ${std_msgs_TARGETS})
# add_dependencies(serial_module my_delta_robot_generate_messages_cpp ${catkin_EXPORTED_TARGETS})

add_executable(draw src/draw.cpp)
target_link_libraries(draw ${std_msgs_TARGETS})
# add_dependencies(draw my_delta_robot_generate_messages_cpp ${catkin_EXPORTED_TARGETS})

add_executable(test_acuracy src/test_acuracy.cpp)
target_link_libraries(test_acuracy ${std_msgs_TARGETS})
# add_dependencies(test_acuracy my_delta_robot_generate_messages_cpp ${catkin_EXPORTED_TARGETS})

ament_package()